# Database Configuration (Neon PostgreSQL)
# Get this from https://neon.tech after creating a project
DATABASE_URL=postgresql://user:password@ep-xxx.neon.tech/neondb?sslmode=require

# JWT Secret (must be minimum 32 characters)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# IMPORTANT: This MUST match BETTER_AUTH_SECRET in frontend
JWT_SECRET=your-super-secret-jwt-key-min-32-characters-here-change-this

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,https://your-app.vercel.app
